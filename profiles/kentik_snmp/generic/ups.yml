# This profile is built to capture all UPS devices that only respond on the global UPS-MIB
---
extends:
  - system-mib.yml
  - ups-mib.yml

provider: kentik-ups

sysobjectid:
  - 1.3.6.1.2.1.33    # Generic UPS
